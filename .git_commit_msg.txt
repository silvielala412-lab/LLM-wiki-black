Fix service identity canonicalization: correct unicode, discriminator protection, 28 tests

1. FIX: BRAND_SUFFIX_PATTERNS had corrupted Chinese characters
   臺享家医 -> 臻享家医 / 绡通 -> 绿通

2. FIX: Brand suffix stripping only applied to title, not service_name
   inferStableInsuranceDedupKey() now uses canonicalServiceIdentityName()
   on IDENTITY_NORMALIZE_FIELDS = {title, service_name}

3. ADD: CONFIRMED_SYNONYMS whitelist
   家庭医生 -> 家庭医生服务
   重疾专案 -> 重疾专案管理

4. ADD: SERVICE_DISCRIMINATORS protection (19 words)
   康复门诊协助 != 康复住院协助 (门诊 != 住院) -> no merge

5. ADD: canonicalServiceIdentityName() exported function

6. ADD: 28 unit tests, all pass
